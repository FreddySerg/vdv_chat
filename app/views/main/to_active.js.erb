$(".active_tr").remove();
$(".line").before("<%= escape_javascript(render :partial => 'text_active', :collection => @messages[:active], :as => :item) %>");
$(".unactive").remove();
$(".line").after("<%= escape_javascript(render :partial => 'text_unactive', :collection => @messages[:unactive], :as => :item) %>");